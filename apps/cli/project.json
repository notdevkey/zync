{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/cli",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/cli",
        "commands": [{ "command": "wasm-pack build" }]
      }
    }
  },
  "tags": []
}
